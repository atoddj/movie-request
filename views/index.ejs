<!DOCTYPE html>
<html lang="en">
<head>
  <% include partials/head %>
</head>
<body>
  <% include partials/navbar %>
  <div class="container">
    <h1>Updates</h1>
      <% for(var i=0; i<posts.length; i++) { %>
        <h2 class="post-title"><%= posts[i].title %></h2>
        <div class="post-author"><%=posts[i].author %></div>
        <div class="post-body"><%= posts[i].body %></div>
      <% } %>
  </div>
<% include partials/js %>
</body>
</html>
